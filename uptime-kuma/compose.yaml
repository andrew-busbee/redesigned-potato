version: '3.3'

services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest # was just 1
    container_name: uptime-kuma
    volumes:
      #- uptime-kuma:/app/data
      - ${DATAPATH}:/app/data
    ports:
      - 8010:3001 
    restart: always

#volumes:
#  uptime-kuma:
